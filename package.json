{
  "name": "pkg-run",
  "version": "1.0.0",
  "description": "executes commands on packages in parallel, but is aware of the dependencies between them",
  "main": "./build/index.js",
  "author": "Andrej T.",
  "license": "MIT",
  "private": true,
  "bin": {
    "pkg-run": "./build/index.js"
  },
  "devDependencies": {
    "yargs": "^10.0.3"
  },
  "scripts": {
    "build": "echo 'no build script defined'"
  }
}
